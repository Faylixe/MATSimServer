/**
 * <p>Runtime package contains all MATSim execution related classes.
 * More particularly, following singleton are exposed :</p>
 * <ul>
 * 	<li>SimulationWorkbench which acts as a container for our simulation related project.</li>
 * 	<li>MATSimRuntime which provides functionality for running a simulation project.</li>
 * </ul>
 */
package org.matsim.server.runtime;